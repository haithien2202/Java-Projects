    public static String version() {
        // lambas + concurrent drawing + marking + styled output
        // this version also fixes the Key listener to recognise the arrow keys on mac OS 
        return "ecs100 library version March 2018";
    } // end of version
